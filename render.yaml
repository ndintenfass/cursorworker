services:
  - type: web
    name: cursor-worker
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    healthCheckPath: /healthz
    envVars:
      - key: CURSOR_API_KEY
        sync: false
      - key: CURSOR_REPO_URL
        sync: false
      - key: GIT_TOKEN
        sync: false
